name: 'cache codecov'
description: 'cache codecov'
runs:
  using: "composite"
  steps:
    - name: Cache codecov
      id: codecov-cache
      uses: actions/cache@v3
      with:
        path: codecov
        key: codecov-${{ runner.os }}-${{ runner.arch }}-1
